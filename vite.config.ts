import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
});
```

---

Add all four via **Add file → Create new file** in your repo, then go back to:
```
stackblitz.com/github/veltscout/perion-signatures
